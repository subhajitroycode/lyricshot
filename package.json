{
  "name": "lyrics-capture",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf ./.cache && rm -rf ./dist && parcel src/index.html",
    "build": "rm -rf ./public && rm -rf ./.cache && rm -rf ./dist && parcel build src/index.html --out-dir public --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.52.3"
  },
  "dependencies": {
    "dom-to-image": "^2.6.0",
    "html-element-to-image": "^2.1.1",
    "html2canvas": "^1.4.1"
  }
}